import { Server } from "ws";

import { roomdb } from './db';

export function processOwnerMessage(wss: Server, code: string, msg: string) {

}

export function processPlayerMessage(wss: Server, code: string, msg: string) {

}